---
- name: Install apache2 package
  ansible.builtin.apt:
    name: apache2
    state: present
    update_cache: true

- name: Start apache2
  ansible.builtin.service:
    name: apache2
    state: started
    enabled: true

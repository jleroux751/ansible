---
- name: Install packages thru loop
  hosts: localhost
  become: true

  vars:
    packages:
      - ftp
      - telnet
      - htop

  tasks:
    - name: Install packages
      ansible.builtin.apt:
        name: "{{ packages }}"
        state: present
